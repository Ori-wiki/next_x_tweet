# Next.js Pages Router guard

This directory is intentionally empty apart from this file.

The project uses the Next.js App Router from the root `app` directory. Keeping
this root `pages` directory prevents Next.js from treating the FSD layer at
`src/pages` as a Pages Router directory.
