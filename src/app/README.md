# App layer

This is the Feature-Sliced Design `app` layer. It contains application-wide
infrastructure such as global styles, providers, and Route Handler
implementations.

Next.js route files live separately in the root `app` directory.
